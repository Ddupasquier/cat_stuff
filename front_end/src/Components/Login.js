import React from "react";
// import { useState } from "react";

function Login({}) {

  return (
    <form className="loginform smallfont">
      <input type="text" />
      <button className="logoutbtn">Login</button>
      <button className="logoutbtn">Logout</button>
    </form>
  );
}

export default Login;
